/**
 * SheShield — Shared API configuration
 *
 * UPDATE THIS IP when the backend machine's IP changes.
 * Run `ipconfig` (Windows) or `ifconfig` (Mac/Linux) on the backend
 * machine to find the correct IP. The phone and laptop must be on
 * the same Wi-Fi network.
 */
export const API_URL = 'http://172.16.20.185:5000';
